Fix so the timer stops when intervals are over
Fix so the Interval number stats are viewed correctly in stats.qml
Fix so you can open firefow with a button
Fix so there is a shutdown button and routine connected to intervals

